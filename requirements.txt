# ── Orchestration & RAG ──────────────────────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0
langchain-ollama>=0.2.0
langgraph>=0.2.0

# ── Vector Store ──────────────────────────────────────────────────────────────
faiss-cpu>=1.8.0          # swap to faiss-gpu if CUDA available

# ── Embeddings & Transformers ─────────────────────────────────────────────────
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.3.0
tokenizers>=0.19.0
accelerate>=0.33.0
huggingface-hub>=0.24.0

# ── Keyword Retrieval ─────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Data Ingestion ────────────────────────────────────────────────────────────
sec-edgar-downloader>=5.0.0
biopython>=1.84
pdfplumber>=0.11.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
requests>=2.32.0
ddgs>=9.14.2

# ── Fine-tuning (run on Colab/HPC — comment out if CPU-only) ─────────────────
# unsloth              # install via: pip install unsloth (Colab)
peft>=0.12.0
bitsandbytes>=0.43.0
trl>=0.10.0
datasets>=2.20.0

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.1.20
langsmith>=0.1.100

# ── API clients ───────────────────────────────────────────────────────────────
groq>=0.9.0
anthropic>=0.34.0         # optional — for Claude-based QA generation

# ── UI & API ──────────────────────────────────────────────────────────────────
streamlit>=1.38.0
fastapi>=0.115.0
uvicorn>=0.30.0
plotly>=5.23.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pandas>=2.2.0
numpy>=1.26.0
tqdm>=4.66.0
jsonlines>=4.0.0
